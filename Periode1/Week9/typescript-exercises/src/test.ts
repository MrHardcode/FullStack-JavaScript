// let path = require("path");
// let fetch = require("node-fetch");

// path.

// const promise = fetch("https://swapi.co/api/people/1/")

// promise
// .then(res => res.json())
// .then(res => {
//     console.log(res);
// })
/**
 * I tried to write and run different bits of code but I couldn't get
 * anything from node to work. There is no intellisense for node (there is 
 * but it doens't work on my pc) which is also extremely frustrating
 */
